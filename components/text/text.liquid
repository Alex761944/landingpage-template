{% doc %}
  Text

  @param {string} [as] - The HTML tag to use. Defaults to p
  @param {string} text - text
  @param {string} variant - Text variant ('HeadingLarge', 'HeadingMedium', 'HeadingSmall', 'BodyLarge', 'BodyMedium', 'BodySmall'). Default: "BodyMedium"
{% enddoc %}

{% assign as = as | default: 'p' %}
{% assign variant = variant | default: 'BodyMedium' %}

<{{ as }} class="Text Text--{{ variant }}">
  {{ text }}
</{{ as }}>
